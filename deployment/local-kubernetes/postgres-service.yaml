apiVersion: v1
kind: Service
metadata:
  name: ittrap-postgres-nodeport
spec:
  type: NodePort
  selector:
    app: ittrap-postgres-service
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 30432
---
apiVersion: v1
kind: Service
metadata:
  name: ittrap-postgres
spec:
  type: ClusterIP
  selector:
    app: ittrap-postgres-service
  ports:
  - port: 5432
    targetPort: 5432
