{{- if .Values.services.inventoryService.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ittrap-inventory-service
  namespace: {{ include "ittrap.namespace" . }}
  {{- if and .Values.global.irsa.enabled .Values.services.inventoryService.useIrsa .Values.global.irsa.roleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.global.irsa.roleArn | quote }}
  {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ittrap-inventory-service
  namespace: {{ include "ittrap.namespace" . }}
  labels:
    app.kubernetes.io/name: ittrap
    app.kubernetes.io/component: inventory-service
spec:
  replicas: {{ .Values.services.inventoryService.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: ittrap
      app.kubernetes.io/component: inventory-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ittrap
        app.kubernetes.io/component: inventory-service
    spec:
      serviceAccountName: ittrap-inventory-service
      containers:
        - name: ittrap-inventory-service
          image: "{{ .Values.services.inventoryService.image.repository }}:{{ .Values.services.inventoryService.image.tag }}"
          imagePullPolicy: {{ .Values.services.inventoryService.image.pullPolicy }}
          ports:
            - containerPort: 80
            - containerPort: 81
          env:
            - name: HTTP_PORT
              value: "80"
            - name: GRPC_PORT
              value: "81"
            - name: ConnectionStrings__PostgreSqlConnection
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secrets.inventoryServiceSecretName }}
                  key: postgreSqlConnectionString
            - name: ConnectionStrings__MongoDbConnection
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secrets.inventoryServiceSecretName }}
                  key: mongoDbConnectionString
            - name: GlobalConfig__Domain
              valueFrom:
                configMapKeyRef:
                  name: global-config
                  key: domain
            - name: GlobalConfig__JwtPrivateKey
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secrets.globalConfigSecretName }}
                  key: jwt-private-key
            - name: GlobalConfig__MediatrLicenseKey
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secrets.globalConfigSecretName }}
                  key: mediatr-license-key
            - name: GlobalConfig__OutboundSnsTopicArn
              valueFrom:
                configMapKeyRef:
                  name: inventory-service-config
                  key: outbound-sns-topic-arn
            - name: AWS__Region
              valueFrom:
                configMapKeyRef:
                  name: global-config
                  key: aws-default-region
            - name: AWS_DEFAULT_REGION
              valueFrom:
                configMapKeyRef:
                  name: global-config
                  key: aws-default-region
          resources:
{{ toYaml .Values.services.inventoryService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: ittrap-inventory-service
  namespace: {{ include "ittrap.namespace" . }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ittrap
    app.kubernetes.io/component: inventory-service
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: grpc
      port: 81
      targetPort: 81
{{- end }}

