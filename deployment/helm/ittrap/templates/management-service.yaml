apiVersion: v1
kind: ServiceAccount
metadata:
  name: ittrap-management-service
  namespace: {{ .Values.global.namespace }}
  {{- if and .Values.aws.irsa.enabled .Values.aws.irsa.roleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.aws.irsa.roleArn | quote }}
  {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ittrap-management-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: ittrap
    app.kubernetes.io/component: management-service
spec:
  replicas: {{ .Values.services.managementService.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: ittrap
      app.kubernetes.io/component: management-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ittrap
        app.kubernetes.io/component: management-service
    spec:
      serviceAccountName: ittrap-management-service
      containers:
        - name: ittrap-management-service
          image: "{{ .Values.global.containerRegistry }}/{{ .Values.services.managementService.image.containerName }}:{{ .Values.services.managementService.image.tag }}"
          imagePullPolicy: {{ .Values.global.pullPolicy }}
          ports:
            - containerPort: 80
            - containerPort: 81
          env:
            - name: HTTP_PORT
              value: "80"
            - name: GRPC_PORT
              value: "81"
            - name: AWS_DEFAULT_REGION
              valueFrom:
                secretKeyRef:
                  name: aws-account-info
                  key: region
            - name: AWS_ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: aws-account-info
                  key: account-id
            - name: ConnectionStrings__PostgreSqlConnection
              valueFrom:
                secretKeyRef:
                  name: management-service-config
                  key: postgreSqlConnectionString
            - name: GlobalConfig__Domain
              valueFrom:
                configMapKeyRef:
                  name: global-config
                  key: domain
            - name: GlobalConfig__JwtPrivateKey
              valueFrom:
                secretKeyRef:
                  name: global-config
                  key: jwt-private-key
            - name: GlobalConfig__MediatrLicenseKey
              valueFrom:
                secretKeyRef:
                  name: global-config
                  key: mediatr-license-key
            - name: GlobalConfig__SqsUrl
              value: {{ include "ittrap.sqsQueueUrl" (dict "global" .Values "queueName" .Values.aws.sqs.managementQueue) | quote }}
            - name: GlobalConfig__OutboundSnsTopicArn
              value: {{ include "ittrap.snsTopicArn" (dict "global" .Values "topicName" .Values.aws.sns.managementEventsTopic) | quote }}
            - name: AWS__Region
              valueFrom:
                secretKeyRef:
                  name: aws-account-info
                  key: region
            - name: AWS__ServiceURL
              valueFrom:
                configMapKeyRef:
                  name: global-config
                  key: aws-service-url
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: access-key-id
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: secret-access-key
          resources:
{{ toYaml .Values.services.managementService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: ittrap-management-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ittrap
    app.kubernetes.io/component: management-service
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: grpc
      port: 81
      targetPort: 81
