apiVersion: apps/v1
kind: Deployment
metadata:
  name: ittrap-identity-postgres-service-deployment
spec:
  selector:
    matchLabels:
      app: ittrap-identity-postgres-service
  template:
    metadata:
      labels:
        app: ittrap-identity-postgres-service
    spec:
      containers:
      - name: ittrap-identity-postgres-service
        image: postgres:latest
        resources:
          requests:
            memory: "512Mi"
            cpu: "150m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER_FILE
          value: "/secrets/postgres-user"
        - name: POSTGRES_PASSWORD_FILE
          value: "/secrets/postgres-password"
        - name: POSTGRES_DB_FILE
          value: "/secrets/postgres-database"
        volumeMounts:
        - name: postgres-credentials
          mountPath: /secrets
      volumes:
      - name: postgres-credentials
        secret:
          secretName: identity-postgres-config
---
apiVersion: v1
kind: Service
metadata:
  name: ittrap-identity-postgres-nodeport
spec:
  type: NodePort
  selector:
    app: ittrap-identity-postgres-service
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 30432
---
apiVersion: v1
kind: Service
metadata:
  name: ittrap-identity-postgres
spec:
  type: ClusterIP
  selector:
    app: ittrap-identity-postgres-service
  ports:
  - port: 5432
    targetPort: 5432